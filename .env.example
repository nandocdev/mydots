# ============================================
# Qtile Configuration - Environment Variables
# ============================================
# Copia este archivo a .env y completa con tus datos:
#   cp env.example .env
#   nano .env
#
# IMPORTANTE: El archivo .env está en .gitignore y NO se subirá a git
# Todas las variables son opcionales y tienen valores por defecto si no se configuran

# ============================================
# EMAIL CONFIGURATION (IMAP Widget)
# ============================================
# Configuración para el widget de email. Si no se configuran, el widget no aparecerá.
# Para usar el widget de email:
#   1. Configura estas variables
#   2. Guarda la contraseña en keyring:
#      python -c "import keyring; keyring.set_password('EMAIL_KEYRING_SERVICE', 'EMAIL_USER', 'tu-contraseña')"
EMAIL_USER=
EMAIL_SERVER=
EMAIL_MBOX=INBOX
EMAIL_KEYRING_SERVICE=
# Ejemplo para Gmail:
# EMAIL_USER=usuario@gmail.com
# EMAIL_SERVER=imap.gmail.com
# EMAIL_KEYRING_SERVICE=imap.gmail.com
# Ejemplo para servidor corporativo:
# EMAIL_USER=usuario@empresa.com
# EMAIL_SERVER=mail.empresa.com
# EMAIL_KEYRING_SERVICE=mail.empresa.com

# ============================================
# PATHS
# ============================================
# Ruta absoluta del directorio de configuración de Qtile
# Ajusta a tu usuario: /home/TU_USUARIO/.config/qtile
QTILE_CONFIG_PATH=/home/USER/.config/qtile
# Ruta para guardar screenshots (puede usar ~/ para home)
SCREENSHOTS_PATH=~/Screenshots
# Ruta de wallpapers (directorio con imágenes de fondo)
WALLPAPERS_PATH=/usr/share/backgrounds/phast

# ============================================
# APPLICATIONS
# ============================================
# Terminal por defecto (kitty, alacritty, urxvt, xterm, etc.)
TERMINAL=kitty
# Navegador web (brave, firefox, chromium, google-chrome, etc.)
BROWSER=brave
# Gestor de archivos (thunar, nautilus, dolphin, pcmanfm, etc.)
FILE_MANAGER=thunar
# Editor de texto (vim, nano, code, gedit, etc.)
EDITOR=vim
# Cliente de email (evolution, thunderbird, mailspring, etc.)
# Se usa para abrir el cliente desde el widget de email
EMAIL_CLIENT=evolution

# ============================================
# HARDWARE CONFIGURATION
# ============================================
# Nombre del dispositivo de backlight (brillo de pantalla)
# Para encontrar el tuyo: ls /sys/class/backlight/
# Opciones comunes:
#   - amdgpu_bl1 (AMD GPU)
#   - intel_backlight (Intel)
#   - acpi_video0 (genérico)
BACKLIGHT_NAME=amdgpu_bl1
# Dispositivo de disco para el widget de disco
# Para encontrar el tuyo: lsblk
# Opciones comunes:
#   - nvme0n1 (NVMe SSD)
#   - sda (SATA/HDD)
#   - mmcblk0 (eMMC)
DISK_DEVICE=nvme0n1
# Interfaz de red (dejar vacío para auto-detectar)
# Para encontrar interfaces: ip link show
# Opciones comunes:
#   - wlan0, wlp2s0 (WiFi)
#   - enp3s0, eth0 (Ethernet)
# Si se deja vacío, se detecta automáticamente la interfaz activa
NETWORK_INTERFACE=

# ============================================
# THEME CONFIGURATION
# ============================================
# Tema por defecto (se usa si no hay tema guardado en .theme)
# Temas disponibles:
#   - catppuccin (default, moderno y suave)
#   - gruvbox (tono cálido)
#   - dracula (oscuro y vibrante)
#   - nord (frío y minimalista)
#   - solarized-dark (clásico y legible)
#   - one-dark (inspirado en Atom)
#   - ayu-dark (profesional)
#   - tokyo-night (nocturno y moderno)
DEFAULT_THEME=catppuccin
# Usar tema aleatorio al inicio (true/false)
# Si es true, ignora DEFAULT_THEME y selecciona uno aleatorio
USE_RANDOM_THEME=true

# ============================================
# DISPLAY CONFIGURATION
# ============================================
# Configuración de monitores con xrandr
# Dejar vacío para usar configuración por defecto
# Ejemplo para dos monitores:
# XRANDR_COMMAND=xrandr --output eDP-1 --primary --mode 1920x1080 --pos 0x0 --rotate normal --output HDMI-1 --mode 1920x1080 --pos 1920x0 --rotate normal --output DP-1 --off
# Ejemplo para un solo monitor:
# XRANDR_COMMAND=xrandr --output eDP-1 --primary --mode 1920x1080
XRANDR_COMMAND=
# Backend de picom (compositor de ventanas)
# Opciones:
#   - --experimental-backends (recomendado, más rápido)
#   - --backend glx (OpenGL, más compatible)
#   - --backend xrender (fallback, más lento)
PICOM_BACKEND=--experimental-backends

# ============================================
# KEYBOARD LAYOUTS
# ============================================
# Layouts de teclado disponibles (separados por comas)
# Estos son los layouts que aparecerán en el widget de teclado
# Para ver layouts disponibles: localectl list-keymaps
# Ejemplos:
#   - latam,es (Español Latinoamérica y España)
#   - us,fr,de (Inglés, Francés, Alemán)
#   - us (solo Inglés)
KEYBOARD_LAYOUTS=latam,es

# ============================================
# SESSION MENU
# ============================================
# Comando para bloquear la pantalla
# Opciones comunes:
#   - light-locker-command -l (light-locker)
#   - i3lock (i3lock)
#   - slock (simple lock)
#   - xlock (xlockmore)
#   - xscreensaver-command -lock (xscreensaver)
LOCK_COMMAND=light-locker-command -l

